# Server-only. Never prefix this with NEXT_PUBLIC_ or VITE_.
OPENROUTER_API_KEY=replace-with-your-openrouter-key
# Optional; defaults to OPENROUTER_API_KEY. Changing it signs users out.
HUMANO_CONSENT_SECRET=replace-with-a-long-random-secret
